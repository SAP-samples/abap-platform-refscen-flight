projection;
strict ( 2 );
use draft;
use recommendations;
use side effects;

define behavior for /dmo/c_traveltp_rec alias Travel
use etag
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}